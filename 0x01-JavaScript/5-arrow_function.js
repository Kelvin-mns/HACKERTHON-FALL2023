// creating an array of numbers
let numbers = [1, 2, 3, 4, 5, 6];
// use the map method and arrow function to create a new array that contains square numbers.
let squares = numbers.map(num => num * num);

console.log(squares);